# TreeHole 生产环境配置
# 部署时请根据实际情况修改以下配置

# ==================== CORS配置 ====================
# 设置你的域名,多个域名用逗号分隔
# 例如: CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# 如果使用IP访问: CORS_ORIGINS=http://123.456.78.90:5173
CORS_ORIGINS=https://your-domain.com,http://your-domain.com

# ==================== 数据库配置 ====================
# SQLite数据库文件路径 (相对于backend目录)
DATABASE_URL=sqlite:///./treehole.db

# ==================== 留言配置 ====================
# 留言最大字符数
MAX_CONTENT_LENGTH=140
# 默认分页大小
DEFAULT_PAGE_SIZE=20
# 最大分页大小
MAX_PAGE_SIZE=100

# ==================== 前端配置 ====================
# 前端API地址 (前端需要通过此地址访问后端)
VITE_API_BASE_URL=http://your-domain.com:8000
